function exportReport(type: string, report: any) {
  if (type === 'pdf') {
    // generate pdf logic
  } else if (type === 'excel') {
    // generate excel logic
  } else if (type === 'html') {
    // build html logic
  }

  return 'result';
}
